Attendant

Attendant(Vendor) is a global entry point ok the kiosk. It's a singleton which provides access to business operations
such as:
1) purchasing
2) receipt

Vendor able to process only one order in a moment. 